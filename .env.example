PORT=8080
HOST=0.0.0.0

# Firestore configuration (Cloud Run service account credentials are used by default)
FIREBASE_PROJECT_ID=your-gcp-project-id

# Optional future internal API integration
APP_API_BASE_URL=https://internal-api.example.com

# Optional MCP metadata overrides
MCP_SERVER_NAME=remote-business-partner-mcp
MCP_SERVER_VERSION=0.1.0
